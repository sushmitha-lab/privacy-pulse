# Rename to .env and fill in your key
# Get a free key at https://console.groq.com — no card required
GROQ_API_KEY=your-groq-key-here

# No other API keys needed — Pwned Passwords endpoint is free, no key required
